name: Deploy to Vercel (Development)

on:
  push:
    branches:
      - dev 
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Vercel Deployment
        run: curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_RBPZUJXRvUSUfbRA2FIXBotHCiFt/jpWnP4M1Xh"